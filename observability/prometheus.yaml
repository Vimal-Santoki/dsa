global:
    scrape_interval: 15s

rule_files:
    - "/etc/prometheus/prometheus.rules.yaml" # <--- Load the rules

scrape_configs:
    - job_name: "otel-collector"
      static_configs:
          - targets: ["otel-collector:8889"]
